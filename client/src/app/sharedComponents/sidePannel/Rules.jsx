const propTypes = {};

const defaultProps = {};

const Rules = () => {
    return (
        <div>
            <div>These are the rules of the game.</div>
            <div>You win when you have no more cards</div>
        </div>
    );
};

Rules.propTypes = propTypes;
Rules.defaultProps = defaultProps;

export default Rules;
